import styled from 'styled-components';
import { PrimaryColor } from './variables';

export const PrimaryBorder = styled.polygon`
  fill: ${PrimaryColor};
`;

export const GrayBorder = styled.polygon`
  fill: dimgray;
`;
